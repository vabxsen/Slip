import { PagePlaceholder } from '@/components/PagePlaceholder';

export function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Theme, device name, downloads, notifications, and trusted devices arrive in Phase 10."
    />
  );
}
