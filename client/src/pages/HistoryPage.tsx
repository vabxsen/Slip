import { PagePlaceholder } from '@/components/PagePlaceholder';

export function HistoryPage() {
  return (
    <PagePlaceholder
      title="History"
      description="Searchable, filterable transfer history arrives in Phase 9."
    />
  );
}
