/** Max devices per pairing room (host + one joiner). */
export const MAX_ROOM_MEMBERS = 2;

/** How often stale/expired empty rooms are swept from the registry. */
export const ROOM_SWEEP_INTERVAL_MS = 60_000;
